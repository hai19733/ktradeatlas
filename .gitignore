# 调试截图（只排除 assets 根目录下的 PNG，logos/ 里的官方 logo 保留）
/assets/*.png

# 品牌资产例外（必须部署）
!/assets/og-image.png
!/assets/apple-touch-icon.png
!/assets/author-minjun.png
!/assets/favicon.png
!/assets/favicon-16x16.png

# 开发工具脚本（不部署）
/tools/

# macOS 系统文件
.DS_Store
